name: test
channels:
  - conda-forge
  - defaults
  - bioconda
dependencies:
  # Base depends
  - python
  - pip
  - atomium
  # Chimera reimplementation
  - biotite
  - MDAnalysis
  # mmligner
  # theseus
  - muscle
  # Testing
  - pytest
  - pytest-cov
  - codecov
  # docs
  - sphinx
  - sphinx_rtd_theme
