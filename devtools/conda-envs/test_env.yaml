name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - atomium
  - os
  - subprocess

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

