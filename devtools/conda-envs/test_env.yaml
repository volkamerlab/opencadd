name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - atomium
  - sphinx
  - sphinx_rtd_theme

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pylint

    # Pip-only installs
  #- pip:
  #  - codecov

