name: test
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - atomium

    # Theseus
  - muscle

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

