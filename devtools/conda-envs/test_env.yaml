name: test
channels:
- conda-forge
- defaults
dependencies:
    # Base depends
  - python
  - pip
  - atomium
  - sphinx
  - sphinx_rtd_theme

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

