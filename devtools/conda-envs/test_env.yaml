name: test
channels:
- conda-forge
- defaults
dependencies:
    # Base depends
  - python
  - pip
  - atomium
  - sphinx
  - sphinx_rtd_theme

    # Chimera reimplementation
  - biotite
  - MDAnalysis
  - structuralalignment

    # mmligner
  - .base

    # Theseus
  - muscle


  warnings

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

