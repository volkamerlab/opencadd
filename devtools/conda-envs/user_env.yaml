name: opencadd
channels:
  - conda-forge
  - defaults
  - bioconda
dependencies:
  # Base depends
  - python
  - pip
  - pandas
  - matplotlib-base
  - MDAnalysis
  - nglview
  - bravado
  - biopandas
  - rdkit
  - tqdm
  # Chimera reimplementation
  - biotite
  - biopython
  # mmligner
  - mmligner
  # theseus
  - muscle
  - theseus
